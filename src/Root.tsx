import React from "react";
import * as ReactDOM from 'react-dom';
// import { createRoot } from "react-dom/client";
import App from "./App";

const root = ReactDOM.render(<App/>, document.getElementById("main"));
// root.render(<App/>;
